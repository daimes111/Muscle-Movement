const React = require('react')
const Default = require('../layout/Default')

class Login extends React.Component {
    render() {
        return(
            <Default title="Login to Muscle Movement Account">
                <form action="/use`"></form>
            </Default>
        )
    }
}